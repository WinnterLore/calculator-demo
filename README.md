# Calculator

A tiny Python module with basic arithmetic operations.

## Functions

- `add(a, b)` - adds two numbers
- `subtract(a, b)` - subtracts b from a
- `multiply(a, b)` - multiplies two numbers
- `divide(a, b)` - divides a by b

## Running tests

```bash
pytest test_calculator.py
```
